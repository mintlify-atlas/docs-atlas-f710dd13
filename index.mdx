---
title: "melange: Build APK Packages from Source"
sidebarTitle: "Home"
description: "melange is a declarative, pipeline-oriented tool for building APK packages from source code. Designed for Alpine Linux and Wolfi ecosystems."
---

melange lets you build APK packages from source using a simple, declarative YAML configuration. Every step of the build pipeline is fully under your control — from fetching source code to installing binaries — with built-in support for multi-architecture builds, package signing, and integration with [apko](https://github.com/chainguard-dev/apko) for creating minimal OCI container images.

<CardGroup cols={2}>
  <Card title="Quickstart" icon="rocket" href="/quickstart">
    Build your first APK package in minutes with a working example.
  </Card>
  <Card title="Installation" icon="download" href="/installation">
    Install melange via Homebrew, from source, or using the container image.
  </Card>
  <Card title="Build Configuration" icon="file-code" href="/configuration/build-file">
    Learn the full melange YAML schema — packages, environments, pipelines, and subpackages.
  </Card>
  <Card title="Command Reference" icon="terminal" href="/commands/overview">
    Explore every CLI command: build, test, lint, keygen, sign, and more.
  </Card>
</CardGroup>

## Why melange?

Secure software factories need reproducible, auditable build processes. melange lets you define your entire build in version-controlled YAML — no hidden state, no manual steps. Every artifact can be signed, attested, and traced back to its source.

<CardGroup cols={2}>
  <Card title="Pipeline Ecosystem" icon="cubes" href="/pipelines/overview">
    Built-in pipelines for Go, Rust/Cargo, Python, Ruby, Maven, npm, and more.
  </Card>
  <Card title="Multi-Architecture" icon="microchip" href="/guides/multi-arch">
    Build for x86_64, aarch64, armv7, and other architectures via QEMU emulation.
  </Card>
  <Card title="Build Caching" icon="database" href="/guides/build-cache">
    Speed up builds with shared caches for Go modules, pip, npm, Maven, and Composer.
  </Card>
  <Card title="Testing & Linting" icon="circle-check" href="/guides/testing">
    Validate packages with built-in test pipelines and post-build linting rules.
  </Card>
</CardGroup>

## Get started in 3 steps

<Steps>
  <Step title="Install melange">
    Install using Homebrew or pull the container image:
    ```shell
    brew install melange
    # or
    docker run cgr.dev/chainguard/melange version
    ```
  </Step>
  <Step title="Write a build configuration">
    Create a `melange.yaml` file describing your package, build environment, and pipeline steps.
    ```yaml
    package:
      name: hello
      version: 2.12
      epoch: 0
      description: "the GNU hello world program"
    environment:
      contents:
        packages:
          - build-base
          - busybox
    pipeline:
      - uses: fetch
        with:
          uri: https://ftp.gnu.org/gnu/hello/hello-${{package.version}}.tar.gz
          expected-sha256: cf04af86dc085268c5f4470fbae49b18afbc221b78096aab842d934a76bad0ab
      - uses: autoconf/configure
      - uses: autoconf/make
      - uses: autoconf/make-install
      - uses: strip
    ```
  </Step>
  <Step title="Build your package">
    Run `melange build` to produce signed APK files in the `packages/` directory:
    ```shell
    melange keygen
    melange build melange.yaml --signing-key melange.rsa
    ```
  </Step>
</Steps>

## Integration with apko

melange packages are designed to be consumed by [apko](https://github.com/chainguard-dev/apko), which builds minimal OCI container images from APK packages. Together they form a complete, auditable software factory: melange produces the packages, apko assembles them into images.

<Note>
  The majority of melange-built packages target the [Wolfi](https://github.com/wolfi-dev) or [Alpine Linux](https://www.alpinelinux.org/) ecosystems. See the [Build Configuration](/configuration/build-file) reference for all available fields.
</Note>
